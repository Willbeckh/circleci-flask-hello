version: 2.1
orbs:
	python: circleci/python@1.2

workflows:
	sample:
		jobs:
			- build-and-test
jobs:
	build-and-test:
	docker:
		image: cimg/python3.8
	steps:
		- checkout
		- python/install-packages:
			pkg-manager: pip
			# app-dir: ~/test-flask-app
		- run:
			name: Run tests

			# assumes pkg is installed by above cmmnd
			command: pytest